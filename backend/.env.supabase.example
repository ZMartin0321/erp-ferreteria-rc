# ============================================
# CONFIGURACIÓN SUPABASE - BACKEND
# ============================================

# Copia este archivo como .env y completa con tus datos

# Base de Datos PostgreSQL de Supabase
# Obtén estos valores en: Supabase → Settings → Database → Connection string
DB_HOST=db.xxxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASS=tu_password_de_supabase_aqui
DB_NAME=postgres

# JWT Secret (genera uno seguro)
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=cambiar_por_secreto_super_seguro_de_32_caracteres_minimo

# Puerto del servidor
PORT=4000

# Entorno (development, production)
NODE_ENV=production

# URL del frontend (actualizar después del deploy)
# Mientras desarrollas localmente: http://localhost:5173
# En producción con Vercel: https://tu-proyecto.vercel.app
FRONTEND_URL=http://localhost:5173

# ============================================
# NOTAS IMPORTANTES
# ============================================

# 1. NUNCA compartas este archivo en Git (ya está en .gitignore)
# 2. El DB_HOST de Supabase siempre empieza con "db." y termina en ".supabase.co"
# 3. El DB_NAME en Supabase siempre es "postgres" (no cambiar)
# 4. Asegúrate que el password no tenga caracteres especiales sin escapar
# 5. Para producción, configura estas variables en tu plataforma de hosting (Render, Railway, etc.)
