# Backend - ERP Ferretería RC

Instrucciones rápidas:

1. Copiar `.env.example` a `.env` y ajustar las variables.
2. Instalar dependencias: `npm install`.
3. Arrancar en modo desarrollo: `npm run dev`.

Endpoints: ver `src/routes`.
