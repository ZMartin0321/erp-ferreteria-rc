# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================

# PostgreSQL (Supabase, Render, Railway, etc.)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=tu_password_aqui
DB_NAME=erp_ferreteria_rc

# ============================================
# SEGURIDAD
# ============================================

# JWT Secret (genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=cambiar_por_secreto_super_seguro_de_32_caracteres_minimo

# ============================================
# SERVIDOR
# ============================================

# Puerto del servidor
PORT=4000

# Entorno (development, production)
NODE_ENV=development

# URL del frontend (para CORS)
# Desarrollo: http://localhost:5173
# Producción: https://tu-dominio.com
FRONTEND_URL=http://localhost:5173

# ============================================
# NOTAS IMPORTANTES
# ============================================

# 1. NUNCA compartas este archivo en Git (ya está en .gitignore)
# 2. Copia este archivo como .env y completa con tus datos reales
# 3. Para producción, configura estas variables en tu plataforma de hosting
# 4. En Supabase: DB_HOST será "db.xxxxx.supabase.co" y DB_NAME será "postgres"
# 5. Asegúrate de generar un JWT_SECRET único y seguro
